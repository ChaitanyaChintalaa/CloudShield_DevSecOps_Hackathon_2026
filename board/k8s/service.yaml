apiVersion: v1
kind: Service
metadata:
  name: Hack
  namespace: Hack
  labels:
    app: Hack
spec:
  type: LoadBalancer
  selector:
    app: Hack
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8080
