apiVersion: v1
kind: Service
metadata:
  name: train-ticket-service
  namespace: train-ticket
  labels:
    app: train-ticket
spec:
  type: LoadBalancer
  selector:
    app: train-ticket
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8080
